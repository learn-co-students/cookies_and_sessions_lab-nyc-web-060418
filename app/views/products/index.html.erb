<%= form_tag add_product_path, method: 'post' do %>
  <%= text_field_tag :product %>
  <%= submit_tag "add to cart"%>
<% end %>
<% unless cart.empty? %>
  <ol>
  <% cart.each do |item| %>
  <li>  <%= item %></li>
  <% end %>
  </ol>
<% end %>
